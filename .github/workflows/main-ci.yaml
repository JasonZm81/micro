name: main

on:
  push:
    branches:
      - dev
    paths:
      - 'main-env/**'




jobs:
  Deployment:
      uses: JasonZm81/micro/.github/workflows/deployment.yaml@dev
      with:
        appName: "main"