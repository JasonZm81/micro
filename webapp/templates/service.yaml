apiVersion: v1
kind: Service
metadata:
  name: webapp-service
spec:
  type: LoadBalancer
  selector:
    app: {{ .Values.webapp.name }}
  ports:
  - port: 80
    targetPort: {{ .Values.container.port }}